# Resume 50+ preview

Static public preview generated from the Next.js MVP branch.

Note: this GitHub Pages preview is for visual review. The server API route `/api/lead` is not available on static hosting.
